function setup() {
  createCanvas(640, 480); //canvas window size
  // background(250, 250, 100);
  // Stroke(150); borders
  // noStroke(); no borders
  // fill(0);
  // nofill();
  // strokeWeight();
}

function draw() {
  background(250, 250, 100);
  ellipse(50, 50, 80, 80);
  // rect(50, 50, 30, 40);
  // ellipse(x, y, w, h); // (x, y, w, h) => center(x,y)
  // rect(x, y, w, h);  //  (x, y, w, h) => upper left corner(x,y)
  // line(x, y, a, b); // start point (x,y) end point (a, b)
  // point(x,y);
}
